<!--pages/serviceFile/serviceFile.wxml-->
<view class="page">
    <view class="result_bd">
        <!-- <mp-cells>
            <mp-cell wx:for="{{resultList}}" link hover value="{{item.name}}" data-index="{{index}}" 
           bindtap="goToInfo" >
            </mp-cell>
        </mp-cells> -->
        <path-view value="{{value}}"
            btnTxt='选中本项'
            catchtapBtn="selThis" user="{{manager}}" itemId="{{id}}" permit="public" download="{{download}}"></path-view>
    </view>
</view>
